Theorem Name:DefinitionIncreasing Package:Logic{
\let I \be \set;
\let f \be \function;
\state (f is increasing on [I]) \eq (\forall (\tuple(a, b) \in \cartprod(I, I)) \follows ((a >= b) \eq (f(a) >= f(b))));
}
